version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --version
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

